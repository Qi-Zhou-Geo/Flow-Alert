name: flow-alert

channels:
  - pytorch
  - nvidia        # ignored safely on non-Linux / non-GPU systems
  - conda-forge
  - defaults

dependencies:
  # --- Core ---
  - python=3.11.14
  - pip

  # --- Numerical / scientific ---
  - numba=0.61.2
  - numpy=1.26.4
  - scipy=1.16.3
  - pandas=2.3.3
  - h5py
  - statsmodels

  # --- Machine learning ---
  - scikit-learn=1.3.0
  - xgboost=3.1.3
  - joblib=1.5.2

  # --- PyTorch (CPU/GPU auto-select) ---
  - pytorch=2.0.1
  - torchvision
  - torchaudio

  # --- Visualization ---
  - matplotlib=3.10.8
  - seaborn=0.13.2
  - plotly=6.5.0
  - colorcet
  - datashader

  # --- Utilities ---
  - tqdm
  - requests
  - networkx
  - multipledispatch
  - pillow
  - protobuf
  - sqlalchemy
  - werkzeug
  - sqlalchemy
  - werkzeug
  - sympy

  # Seismology
  - obspy=1.4.0
  - pytz=2025.2

  # --- YAML ---
  - pyyaml
  - ruamel.yaml

  # --- pip-only ---
  - pip:
      - torchinfo==1.8.0
